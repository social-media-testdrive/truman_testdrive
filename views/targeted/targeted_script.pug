extends ../script.pug

block post
  if (val.type == user.targetedAdTopic[user.targetedAdTopic.length - 1])
    include ../partials/script/targetedAd
    
  else if (((val.type == 'Sports') || (val.type == 'Food') || (val.type == 'Gaming')) && (!(val.type == user.targetedAdTopic[user.targetedAdTopic.length - 1])))
    //- do nothing, do not display. This is another targteted ad, but one of the unselected types 
    
  else
  
    include ../partials/script/actorPost
  
  
block scripts2
  script.
    /* alert('hi'); */
