extends ../ui_layout

block content
  - var cdn = "https://dhpd030vnpk29.cloudfront.net";
  .computer.only.tablet.only.four.wide.column 
  #content.eight.wide.computer.ten.wide.tablet.sixteen.wide.mobile.column
    .ui.one.column.centered.grid.feedbackMessagesGrid
      .ui.fixed.bottom.sticky
        #clickSiguienteHelpMessage.ui.icon.compact.hidden.message.warning
          i.close.icon
          i.question.icon.float
          .content
            .ui.header ¿Necesitas ayuda?
            p Subir y Dale clic a "Siguiente" para comenzar

    #step3.ui.fluid.card.test
      .content
        .right.floated.meta= "Ayer"
        |

        a(href="/user/andrewthomas_")
          img.ui.avatar.image(src=cdn + "/profile_pictures/user71.JPG")
          span= "Andrés Torres"
      .image
        img.post(src=cdn + "/post_pictures/digfootga1.jpg")
      #step4.content
        .description
          | un día después de mi cumpleaños y adivina qué tengo que hacer. Limpia tu cuarto dice mi mamá, la odio!!!

      .three.ui.bottom.attached.icon.buttons
        .ui.reply.button(tabindex="0")
          i.reply.icon
          |
          | Responder

        .ui.flag.button(tabindex="0")
          i.flag.icon
          |
          | Reportar

        .ui.labeled.button(tabindex="0")
          .ui.like.button
            i.heart.icon
            |
            | Like

          a.ui.basic.red.left.pointing.label.count= 2

      .content
        .ui.comments
          .comment
            a.avatar
              img(src=cdn + "/profile_pictures/user52.jpg")
            .content
              a.author Mateo Muñoz
              .metadata
                span.date hace 5 horas
                .rating
                  i.heart.icon
                  span.num= 1
                  |
                  | Likes

              .text
                | jaja, eso apesta

              .actions
                a.comment.like Like
                a.comment.flag Reportar
      .extra.content
        .ui.fluid.left.labeled.right.icon.input
          .ui.label
            img.ui.avatar.image(
              src=cdn + "/profile_pictures/" + user.profile.picture,
              name=user.profile.name
            )
          input.newcomment(type="text", placeholder="Write a Comment")
          i.big.send.link.icon

    #step5.ui.fluid.card.test.addBottomMargin
      .content
        .right.floated.meta= "15 Minutes Ago"
        |

        a(href="/user/Suzieee")
          img.ui.avatar.image(src=cdn + "/profile_pictures/user40.jpg")
          span= "Susana Tapia"
      .image
        img.post(src=cdn + "/post_pictures/digfootga3.jpg")
      .content
        .description= "OPD Lucía, te quiero, pero te ves ridícula. LOL"
      .three.ui.bottom.attached.icon.buttons
        .ui.reply.button(tabindex="0")
          i.reply.icon
          |
          | Responder

        .ui.flag.button(tabindex="0")
          i.flag.icon
          |
          | Reportar

        .ui.labeled.button(tabindex="0")
          .ui.like.button
            i.heart.icon
            |
            | Like

          a.ui.basic.red.left.pointing.label.count= 2

      .content
        .ui.comments
          .comment
            a.avatar
              img(src=cdn + "/profile_pictures/user66.jpeg")
            .content
              a.author Michelle Ríos
              .metadata
                span.date Hace 1 minuto
                .rating
                  i.heart.icon
                  span.num= 0
                  |
                  | Likes

              .text
                | lol!!

              .actions
                a.comment.like Like
                a.comment.flag Reportar
      .extra.content
        .ui.fluid.left.labeled.right.icon.input
          .ui.label
            img.ui.avatar.image(
              src=cdn + "/profile_pictures/" + user.profile.picture,
              name=user.profile.name
            )
          input.newcomment(type="text", placeholder="Write a Comment")
          i.big.send.link.icon

block scripts
  script(src="/postFunctionalities.js")
  script(src="/module_js/digfoot-esp/digfoot-esp_tutorial.js")
  script(src="/module_js/voiceover.js")
  script(src="/module_js/base_introSteps.js") 
  script(src="/module_js/base_tutorial.js")
